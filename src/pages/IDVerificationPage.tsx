import IDVerification from "../components/verification/IDVerification";

export default function IDVerificationPage() {
    return (
        <section className="flex items-center justify-center py-16 px-4 min-h-screen bg-gray-50">
            <IDVerification />
        </section>
    );
}